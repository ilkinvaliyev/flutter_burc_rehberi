import 'package:flutter/material.dart';

class BurcLists extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Burc Rehberi", style: TextStyle(fontSize: 22),textAlign: TextAlign.center,),
      ),
    );
  }
}
